using VulkanEngine.ECS_internals;

namespace VulkanEngine.Phases.FramePreTickPhase;

public static class FramePreTickSequence
{
    public static void Register()
    {
        // var work = new ExecutionUnitBuilder(Work)
        //     .Named("Work")
        //     // .Writes(VKRender.RendererEcsResource)
        //     // .Writes(Input.Input.InputResource)
        //     .Build();
        // ScheduleMaker.RegisterToTarget(work, "framePreTick");
    }
}